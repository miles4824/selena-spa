// =============================================================
// SCREEN: POS SCREEN (Tạo tour & đồng hồ ca gội)
// =============================================================
function initPOSScreen() {
  if (typeof renderComboPills === 'function') renderComboPills();
}
